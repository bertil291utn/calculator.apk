
const calculate = (calculator, buttonName) => {
  const { total, next, operation } = calculator;
  switch (buttonName) {
    case 'A/C':
      
      break;
    case '+/-':
      
      break;
  
    default:
      break;
  }
 
};

export default calculate;